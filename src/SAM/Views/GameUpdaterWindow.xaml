<ui:FluentWindow x:Class="SAM.Views.GameUpdaterWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 mc:Ignorable="d"
                 Title="Game Updater" Height="250" Width="400"
                 WindowStartupLocation="CenterOwner"
                 ResizeMode="NoResize"
                 ExtendsContentIntoTitleBar="True"
                 WindowCornerPreference="Round"
                 WindowBackdropType="Mica">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Title="" Grid.Row="0" />

        <Grid Grid.Row="1" Margin="20">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Enter Steam AppID to update (e.g. 440):" FontSize="14" Margin="0,0,0,10" />
                
                <ui:TextBox x:Name="AppIdTextBox" 
                         Margin="0,0,0,10" />

                <CheckBox x:Name="MirrorCheckBox" 
                          Content="Use mirror source" 
                          Margin="0,0,0,10" />

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <ui:Button Content="Cancel" Click="CancelButton_Click" Margin="0,0,10,0" />
                    <ui:Button Content="Update" Appearance="Primary" Click="UpdateButton_Click" />
                </StackPanel>

                <TextBlock x:Name="StatusTextBlock" 
                           Text="" 
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" 
                           FontSize="12" 
                           Margin="0,10,0,0"
                           TextWrapping="Wrap" />
            </StackPanel>
        </Grid>
    </Grid>
</ui:FluentWindow>
